function SelectWrapper() {
  return (
    <div></div>
  );
}

/*
*,
*::before,
*::after {
  box-sizing: border-box;
}

"box-border before:box-border after:box-border"

.styleReset {
  // A reset of styles, including removing the default dropdown arrow
  appearance: none;
  // Additional resets for further consistency
  background-color: transparent;
  border: none;
  padding: 0 1em 0 0;
  margin: 0;
  width: 100%;
  font-family: inherit;
  font-size: inherit;
  cursor: inherit;
  line-height: inherit;
  outline: none;
}

"appearance-none bg-transparent border-none p-0 pr-1 m-0 w-0 outline-none"

.styleReset::-ms-expand {
  display: none;
}
*/

export default SelectWrapper;